# V3 version policy

- `stage_partition/V2` is frozen as a historical version.
- `stage_partition/V3` takes over the mainline.
- V3 phase-1 only builds a clean stage-partition backbone and original window-level audits.
- `window_catalog.csv` is advisory only and cannot redefine the main windows.
- legacy V2 support / retention outputs are not main results in V3.


Patch A adds yearwise diagnostics and strict-overlap support matching.
