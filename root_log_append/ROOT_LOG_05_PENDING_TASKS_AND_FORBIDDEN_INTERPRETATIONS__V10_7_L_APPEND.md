# ROOT_LOG_05 append — V10.7_l

## Pending checks

1. Confirm `run_meta_v10_7_l.json` shows the intended `n_perm` and `n_boot`.
2. Check `h_metric_direction_audit_v10_7_l.csv` before interpreting signs, especially `H_west_extent_lon_transition`.
3. Inspect P spatial maps and P latitude profiles; do not rely only on metric tables.
4. Check `h_to_p_influence_by_year_v10_7_l.csv` for few-year dominance.

## Forbidden interpretations

- Do not claim H causes P.
- Do not claim H controls W45.
- Do not generalize H→P spatial verification to full W33→W45 mapping.
- Do not treat high/low composites as causal experiments.
- Do not interpret the sign of `H_west_extent_lon_transition` without verifying that smaller longitude means stronger westward extension.
