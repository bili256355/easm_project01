# ROOT_LOG_05 append — V10.7_c HOTFIX01 interpretation guardrails

## Added guardrail

When interpreting V10.7_c spatial results:

- Do not use broad full-domain z500 composites (`10–60N, 20–150E`) as the primary evidence for H-object event content.
- Use the H-object-domain metrics (`15–35N, 110–140E`) for H event-content classification.
- Full-domain composites should be treated as context/background maps, not direct H-object metrics.

## Corrected scale-context guardrail

V10.7_b labels the early ridge as `H19`; V10.7_c labels the event-content window as `H18`. These should be treated as aliases for the early-H event context. Do not interpret the earlier `no_clear_scale_ridge_near_target` value for H18 as evidence against the V10.7_b early ridge unless HOTFIX01 has been rerun and the corrected role summary still says so.

## Still forbidden

V10.7_c HOTFIX01 still does not justify:

- H18 causes or influences H35.
- H35 causes or influences W045.
- H35 is a confirmed weak precursor.
- Any H18/H35/H45 spatial similarity is a physical pathway proof.
