# ROOT_LOG_05 append: V10.7_k HOTFIX03

Do not interpret a scoped `--pairwise-scope h-source` run as a complete all-object pairwise map.

Use it only to verify and formally test current H_E2 structural candidate links, especially:
- H west_extent / zonal_width / edge transition -> M P rainband structure transition
- H state/position -> M Je axis/strength structure

If the full cross-object matrix is needed, run `--pairwise-scope all` separately.
