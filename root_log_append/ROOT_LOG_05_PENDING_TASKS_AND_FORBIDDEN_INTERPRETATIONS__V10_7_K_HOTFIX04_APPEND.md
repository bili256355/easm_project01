# ROOT LOG APPEND — V10.7_k HOTFIX04 interpretation boundary

If `--multivariate-policy skip` or `--object-contribution-policy skip` is used, the run cannot support claims about overall E2→M multivariate mapping skill or remove-one-source object contribution. It only supports H-specific pairwise structural mapping review.

Forbidden interpretation: do not treat skipped multivariate stages as negative results.
