# ==============================================================================
# .gitignore for easm_project01
# ==============================================================================
# Purpose:
# - Keep source code, root-level handoff logs, README/manifest files, and small
#   configuration files under version control.
# - Ignore Python cache, notebook checkpoints, local environments, temporary files,
#   scattered old logs, and large generated scientific outputs.
# - This file does NOT delete existing tracked files. If a file is already tracked,
#   use `git rm --cached <path>` to stop tracking it while keeping it locally.
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. Keep the new root-level consolidated logs tracked
# ------------------------------------------------------------------------------
# These are the replacement root logs intended to remain after old scattered logs
# are deleted or ignored.
!ROOT_LOG_*.md
!ROOT_LOG_INDEX_OLD_LOGS_CONSOLIDATION_TABLE.csv
!README_root_log_consolidation_v1_a.md

# Keep common project documentation / manifests tracked.
!README.md
!**/README.md
!STRUCTURE_MANIFEST.txt
!**/STRUCTURE_MANIFEST.txt
!VERSION_POLICY.md
!**/VERSION_POLICY.md
!DEPENDENCY_MANIFEST.json
!**/DEPENDENCY_MANIFEST.json

# ------------------------------------------------------------------------------
# 2. Python cache and build artifacts
# ------------------------------------------------------------------------------
__pycache__/
**/__pycache__/
*.py[cod]
*$py.class
*.so
*.pyd
.Python
build/
dist/
.eggs/
*.egg-info/
*.egg
pip-wheel-metadata/

# ------------------------------------------------------------------------------
# 3. Jupyter / notebook temporary files
# ------------------------------------------------------------------------------
.ipynb_checkpoints/
**/.ipynb_checkpoints/

# ------------------------------------------------------------------------------
# 4. Local virtual environments and local-only configuration
# ------------------------------------------------------------------------------
.venv/
venv/
env/
ENV/
.env
.env.*
*.env
.python-version

# IDE / editor local files
.idea/
.vscode/
*.code-workspace
*.sublime-project
*.sublime-workspace
.DS_Store
Thumbs.db

# ------------------------------------------------------------------------------
# 5. Temporary / backup files
# ------------------------------------------------------------------------------
*.tmp
*.temp
*.bak
*.backup
*.old
*.orig
*.swp
*.swo
~$*
*~

# ------------------------------------------------------------------------------
# 6. Runtime logs and ad-hoc scattered logs
# ------------------------------------------------------------------------------
# New durable logs should be written at the project root as ROOT_LOG_*.md.
# Old-style scattered logs are ignored to prevent log sprawl from returning.
*.log
**/logs/
**/log/
**/UPDATE_LOG*.md
**/ROOT_PROJECT_LOG*.md
**/CURRENT_ENGINEERING_MANIFEST*.md
**/PATCH_README*.md
**/PATCH_MANIFEST*.md
**/PATCH_FILE_LIST*.txt

# Re-allow root consolidated logs after broad log ignore rules.
!ROOT_LOG_*.md
!ROOT_LOG_INDEX_OLD_LOGS_CONSOLIDATION_TABLE.csv
!README_root_log_consolidation_v1_a.md

# ------------------------------------------------------------------------------
# 7. Large scientific data and generated binary outputs
# ------------------------------------------------------------------------------
# Keep source code and small metadata in Git; keep large input/output arrays local
# unless a specific file is intentionally promoted to version control.
*.nc
*.nc4
*.grib
*.grb
*.grb2
*.h5
*.hdf
*.hdf5
*.zarr/
*.npy
*.npz
*.pkl
*.pickle
*.joblib

# ------------------------------------------------------------------------------
# 8. Generated analysis outputs
# ------------------------------------------------------------------------------
# These can be regenerated from scripts. If a small summary table is meant to be
# preserved, add it explicitly with `git add -f <file>` or place it in a curated
# docs/results-summary location.
**/outputs/
**/output/
**/figures/
**/plots/
**/results/
**/result/
**/artifacts/
**/cache/
**/tmp/
**/temp/

# ------------------------------------------------------------------------------
# 9. Large rendered figures / documents generated by runs
# ------------------------------------------------------------------------------
*.png
*.jpg
*.jpeg
*.tif
*.tiff
*.svg
*.pdf
*.pptx
*.docx
*.xlsx
*.xls

# ------------------------------------------------------------------------------
# 10. Optional: allow curated documentation assets if you intentionally create them
# ------------------------------------------------------------------------------
# Uncomment and adjust these if you later decide to keep selected final figures,
# tables, or documents in Git.
# !shared_docs/**
# !docs/**
# !figures/final/**
# !tables/final/**

# ------------------------------------------------------------------------------
# 11. Safety notes
# ------------------------------------------------------------------------------
# If a file was already committed before being added here, Git will still track it.
# To stop tracking but keep the local copy:
#   git rm --cached <file>
#   git rm -r --cached <directory>
# Then commit the change.
# ==============================================================================
